const swap = require('./array/swap')
const update = require('./array/update')
const add = require('./array/add')
const remove = require('./array/remove')
const create = require('./array/create')

module.exports = {
  array: {
    swap: swap,
    update: update,
    add: add,
    remove: remove,
    create: create
  }
}